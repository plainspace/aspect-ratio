{
  "name": "ratio",
  "scripts": {
    "dev": "next",
    "build": "next build && next export -o dist",
    "now-build": "next build && next export -o dist"
  },
  "dependencies": {
    "@zeit/next-sass": "^1.0.1",
    "next": "^7.0.2",
    "node-sass": "^4.10.0",
    "react": "^16.6.3",
    "react-dom": "^16.6.3"
  }
}
